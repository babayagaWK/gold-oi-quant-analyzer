# วิธีการ Push ไปยัง GitHub และสร้าง APK อัตโนมัติ

## ขั้นตอนที่ 1: สร้าง GitHub Repository

1. ไปที่ https://github.com/new
2. ตั้งชื่อ repository เป็น `gold-oi-quant-analyzer` (หรือชื่ออื่นที่คุณต้องการ)
3. เลือก **Public** (เพื่อให้ GitHub Actions ทำงานฟรี)
4. **ไม่ต้อง** เลือก "Add README" หรือ ".gitignore" หรือ license
5. คลิก "Create repository"

## ขั้นตอนที่ 2: Push Code ขึ้น GitHub

หลังจากสร้าง repository แล้ว ให้รันคำสั่งเหล่านี้ใน PowerShell:

```powershell
# เปลี่ยน YOUR_USERNAME และ YOUR_REPO_NAME ให้ตรงกับของคุณ
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

**ตัวอย่าง:**
```powershell
git remote add origin https://github.com/johndoe/gold-oi-quant-analyzer.git
git branch -M main
git push -u origin main
```

## ขั้นตอนที่ 3: รอให้ GitHub Actions Build APK

1. หลังจาก push เสร็จ ไปที่ repository บน GitHub
2. คลิกที่แท็บ **Actions**
3. คุณจะเห็น workflow "Build Android APK" กำลังทำงาน
4. รอประมาณ 5-10 นาทีจนกว่า build จะเสร็จ (สถานะจะเป็น ✅ สีเขียว)

## ขั้นตอนที่ 4: ดาวน์โหลด APK

มี 2 วิธีในการดาวน์โหลด APK:

### วิธีที่ 1: จาก GitHub Actions Artifacts
1. ไปที่แท็บ **Actions**
2. คลิกที่ workflow run ที่เสร็จแล้ว
3. เลื่อนลงไปหา **Artifacts**
4. คลิกดาวน์โหลด `app-debug`

### วิธีที่ 2: จาก Releases (แนะนำ)
1. ไปที่แท็บ **Releases**
2. คุณจะเห็นไฟล์ `app-debug.apk`
3. คลิกดาวน์โหลด

## ขั้นตอนที่ 5: ติดตั้งบนโทรศัพท์ Android

1. โอนไฟล์ `app-debug.apk` ไปยังโทรศัพท์ Android
2. เปิดไฟล์เพื่อติดตั้ง
3. อนุญาตให้ติดตั้งจากแหล่งที่ไม่รู้จัก (ถ้าถูกถาม)
4. คลิก "ติดตั้ง"

## การอัปเดตแอป

เมื่อคุณแก้ไขโค้ดและต้องการสร้าง APK ใหม่:

```powershell
# แก้ไขโค้ด
# จากนั้น commit และ push

git add .
git commit -m "อธิบายการเปลี่ยนแปลง"
git push
```

GitHub Actions จะ build APK ใหม่ให้อัตโนมัติทุกครั้งที่คุณ push!

## หมายเหตุสำคัญ

- ไฟล์ APK ที่สร้างเป็นแบบ **debug** ไม่ใช่ production
- หากต้องการ APK แบบ release สำหรับเผยแพร่ ต้อง:
  1. สร้าง keystore
  2. เพิ่ม secrets ใน GitHub
  3. แก้ไข workflow ให้ build release APK

## ปัญหาที่อาจพบ

### GitHub Actions ล้มเหลว
- ตรวจสอบ logs ในแท็บ Actions
- อาจเป็นเพราะ dependencies หรือ build error

### APK ติดตั้งไม่ได้
- ตรวจสอบว่าเปิด "อนุญาตให้ติดตั้งจากแหล่งที่ไม่รู้จัก" แล้ว
- ถ้าเคยติดตั้งเวอร์ชันเก่า ลองถอนการติดตั้งก่อน

## คำสั่ง Git ที่มีประโยชน์

```powershell
# ดู status
git status

# ดู remote
git remote -v

# ดู log
git log --oneline

# สร้าง branch ใหม่
git checkout -b feature-name
git push -u origin feature-name
```

---

**สร้างโดย GitHub Copilot** 🤖
