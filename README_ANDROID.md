# Gold OI Quant Analyzer - Android App

แอปพลิเคชัน Android สำหรับวิเคราะห์ความสัมพันธ์ราคาทองคำ Spot และ Open Interest (OI) จาก CME แบบเรียลไทม์

## คุณสมบัติ

- 📊 แสดงกราฟราคาทองคำ Spot และ Futures แบบเรียลไทม์
- 📈 วิเคราะห์ Open Interest (OI) จาก CME
- 🤖 AI Analysis ด้วย Google Gemini API
- 🔄 Auto-update ทุก 60 วินาที
- 📱 รองรับ Android

## การติดตั้ง

### ดาวน์โหลด APK

1. ไปที่ [Releases](../../releases)
2. ดาวน์โหลดไฟล์ `app-debug.apk` เวอร์ชันล่าสุด
3. ติดตั้งบนอุปกรณ์ Android ของคุณ

**หมายเหตุ:** คุณอาจต้องเปิดใช้งาน "อนุญาตให้ติดตั้งจากแหล่งที่ไม่รู้จัก" ในการตั้งค่าของอุปกรณ์

## การพัฒนา

### ความต้องการ

- Node.js 20+
- Android Studio (สำหรับ build APK ในเครื่อง)
- Java JDK 17+

### การติดตั้ง

```bash
# ติดตั้ง dependencies
npm install

# Build web app
npm run build

# Sync กับ Android
npx cap sync android

# เปิด Android Studio
npx cap open android
```

### Build APK

```bash
# Build debug APK
cd android
./gradlew assembleDebug

# APK จะอยู่ที่
# android/app/build/outputs/apk/debug/app-debug.apk
```

## GitHub Actions

โปรเจคนี้ใช้ GitHub Actions สำหรับ auto-build APK เมื่อมีการ push code

ไฟล์ APK จะถูกสร้างอัตโนมัติและอัปโหลดไปที่:
- Artifacts ใน GitHub Actions
- Releases (สำหรับ push ไปที่ main branch)

## การใช้งาน

1. เปิดแอป
2. (ตัวเลือก) ใส่ Google Gemini API Key เพื่อใช้งาน AI Analysis
3. เลือกโหมด:
   - **LIVE AGENT**: ข้อมูลเรียลไทม์จาก TradingView และ CME
   - **SIMULATION**: ข้อมูลจำลองสำหรับทดสอบ
4. เปิด Auto-update เพื่อรับข้อมูลทุก 60 วินาที

## เทคโนโลยีที่ใช้

- React 19
- TypeScript
- Vite
- Capacitor 7
- Recharts (สำหรับกราฟ)
- Google Gemini API
- Tailwind CSS

## License

MIT

## หมายเหตุ

แอปนี้สร้างขึ้นเพื่อใช้ประกอบการวางแผนเทรด Gold CFD บน MT5 เท่านั้น ไม่ใช่คำแนะนำในการลงทุน
