name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: npm install
      
    - name: Build web app
      run: npm run build
      
    - name: Setup Java JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
        
    - name: Install Android SDK components
      run: |
        sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"
        yes | sdkmanager --licenses || true
      
    - name: Sync Capacitor with Android
      run: npx cap sync android
      
    - name: Make gradlew executable
      run: chmod +x android/gradlew
      
    - name: Clean Gradle cache
      run: |
        cd android
        ./gradlew clean --no-daemon
        
    - name: Build Debug APK
      run: |
        cd android
        ./gradlew assembleDebug --no-daemon --warning-mode all
      env:
        GRADLE_OPTS: -Xmx4096m -Dorg.gradle.daemon=false
      
    - name: Upload APK Artifact
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
        
    - name: Create GitHub Release
      if: success() && github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        files: android/app/build/outputs/apk/debug/app-debug.apk
        tag_name: v1.0.${{ github.run_number }}
        name: Gold OI Analyzer v1.0.${{ github.run_number }}
        body: |
          🎉 **Gold OI Quant Analyzer - Android APK**
          
          📱 ดาวน์โหลด `app-debug.apk` ด้านล่างแล้วติดตั้งบนโทรศัพท์ Android
          
          **Build Information:**
          - Build Number: #${{ github.run_number }}
          - Commit: ${{ github.sha }}
          - Date: ${{ github.event.head_commit.timestamp }}
          
          **วิธีติดตั้ง:**
          1. ดาวน์โหลด app-debug.apk
          2. อนุญาต "Install from Unknown Sources" ในการตั้งค่า
          3. เปิดไฟล์และติดตั้ง
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
